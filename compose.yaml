services:
  app:
    build:
      context: ./app
    volumes:
      # - ./app:/app
      - ./app/src:/app/src
    command: sh -c "npm run start"
    # command: sh -c "/bin/bash"
    # tty: true
    ports:
      - "3000:3000"